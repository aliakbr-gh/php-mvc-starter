Options -Indexes
RewriteEngine On

# Existing assets are served directly; everything else uses index.php.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
